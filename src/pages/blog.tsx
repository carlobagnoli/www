import { AnimatedText } from "../animated-text"

export const Blog = () => {
  return (
    <div>
      <AnimatedText delay={0} text="ENTRIES: 0" top="calc(50% - 90px)" left="calc(50% - 180px)" />
    </div>
  )
}
